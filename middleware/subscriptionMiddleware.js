// Arquivo: middleware/subscriptionMiddleware.js
// OBS: Placeholder (o app.js faz require desse middleware, mas ele pode estar desativado no fluxo atual).
// Se você já tem uma implementação real no seu projeto, pode substituir por ela.

module.exports = function requireSubscription(req, res, next) {
  return next();
};
